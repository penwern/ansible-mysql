# SPDX-License-Identifier: MIT-0
---
galaxy_info:
  author: Cameron Lyall
  description: A robust MySQL server role for Ubuntu systems with comprehensive configuration options
  company: Penwern Limited

  # If the issue tracker for your role is not on github, uncomment the
  # next line and provide a value
  # issue_tracker_url: http://example.com/issue/tracker

  # Choose a valid license ID from https://spdx.org - some suggested licenses:
  # - BSD-3-Clause (default)
  # - MIT
  # - GPL-2.0-or-later
  # - GPL-3.0-only
  # - Apache-2.0
  # - CC-BY-4.0
  license: MIT-0

  min_ansible_version: "2.9"

  namespace: penwern
  role_name: mysql

  # If this a Container Enabled role, provide the minimum Ansible Container
  # version.
  # min_ansible_container_version:

  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy

  galaxy_tags:
    - database
    - mysql
    - mariadb
    - ubuntu
    - debian
    - server
    - sql
    - replication
    - backup
    - security

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the
  # '[]' above, if you add dependencies to this list.
